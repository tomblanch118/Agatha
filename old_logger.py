import signal

	def signal_handler(signal, frame):
		
if __name__ == "__main__"
	signal.signal(signal.SIGINT, signal_handler)
#try to send to php

#if fail, store in file/ memory and periodically check network. 
#limit size of stored data 